# reverse-proxy + TLS in front of the API. Point your domain here, then certbot.
server {
    server_name tutor.yourdomain.com;
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
    }
}
