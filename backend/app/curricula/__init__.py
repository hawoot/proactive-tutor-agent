"""Curricula are data, not code: a curriculum module exports PROGRAM (the
program row) and TREE (nested units, each with optional content and skills).
Loaded by app.seed for fresh installs and by scripts/push_curriculum.py to
upsert into a live server over the HTTP API."""
